@{
  Layout = "_Layout";
}

@model Library.Models.Title

<h1>Create a Title</h1>

@* @if (@ViewBag.AuthorId.Count == 0)
{
  <p>You must add an Author before creating a title!</p>
}
else
{ *@
  @using (Html.BeginForm())
  {
    @Html.LabelFor(model => model.Name);
    @Html.TextBoxFor(model => model.Name);
    <br>
    @Html.Label("Select Author");
    @Html.DropDownList("AuthorId");
    <br>
    <input type="submit" value="Add a Title"/>
  }
@* } *@

<p>@Html.ActionLink("Back to List", "Index")</p>